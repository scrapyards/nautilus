[filter:token_auth]
use = egg:nautilus#token_auth

[filter:cors]
use = egg:oslo.middleware
oslo_config_project = nautilus

[pipeline:public_api]
pipeline = cors token_auth

[composite:main]
use = egg:Paste#urlmap
/v1 = api_v1
